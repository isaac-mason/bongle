// editor/project.ts — the OPFS project name, shared so every context that opens
// the working copy (main doc, bundler worker, build worker, realms) agrees on
// the same subdir. One dev project for now.
export const PROJECT_NAME = 'project';
