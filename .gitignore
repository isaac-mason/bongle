dist
node_modules
llm
AGENTS.md
.bongle
tmp
generated
resources
vendor
editor-node-modules.zip
.tsbuild-cache

.DS_Store
**/.DS_Store

# World C dependency prebundle (build artifact, seeded into editor vfs)
deps-dist/

# Assembled Blockbench bundle (build output of `pnpm run blockbench:build`).
# Rebuilt in dev.sh + the website Dockerfile — NOT committed (the hashed asset
# tree would bloat history).
editor/public/static/blockbench/
