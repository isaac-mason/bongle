export type { Config } from '../core/config';
export {
    CONFIG_ID,
    DEFAULT_CONFIG,
    DEFAULT_TICK_RATE,
    HARD_MAX_PLAYERS_PER_ROOM,
    isStandalone,
    MAX_TICK_RATE,
    MIN_TICK_RATE,
    serverMaxPlayers,
    serverTickRate,
} from '../core/config';
export { config } from '../core/registry';
